import React from 'react';

const Product = (props) => {
    const []=props.products
    return (
        <div>
            <h1>THis is product page</h1>
        </div>
    );
};

export default Product;